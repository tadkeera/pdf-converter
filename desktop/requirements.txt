# PDF Converter — desktop application dependencies
pymupdf>=1.24.0          # PDF text + table extraction (fast, handles Arabic)
openpyxl>=3.1.2          # Excel .xlsx writing (RTL, styles)
pillow>=10.0.0           # used for icon handling in the build only
